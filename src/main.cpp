#include <iostream>

#include "neural.cpp"
#include "mnist.cpp"

using namespace std;


int main(){

/*
 *	Add in command line flag stuff here
 *
 *	database location, type of network, number of layers (not yet implemented), number of hidden units per layer, number of epochs, etc
 *
 *	Currently only using 1 hidden layer (for a total of 3)
 */


	cout<<"Loading in data from (add database location here)\n";

	//load data
	


	//choose type of network, initialize appropriately, should probably use functions at some point but might not be needed
	
//	MLP mynetwork;
//	mynetwork.Initialize();
//	mynetwork.Train(dataBase,learningRate, numEpochs,numHiddenUnits);

	cout<<"Training network...\n";

	return 0;
	}





